services:
  liberchat:
    image: liberchat/liberchat:6.1.20
    container_name: liberchat-simple
    ports:
      - "3000:3000"
    environment:
      - ALLOWED_DOMAINS=http://localhost:3000
      - NODE_ENV=development
      - PORT=3000
    restart: unless-stopped